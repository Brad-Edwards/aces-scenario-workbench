# Architecture Decision Records

Architecture Decision Records (ADRs) for the ACES Scenario Workbench live in
this directory. This is the `adr_dir` referenced by `.ground-control.yaml`.

Add one file per decision, numbered sequentially, for example
`001-persistence-architecture.md`.
